function InteractionMenu(x) {
    console.log("Teste", x)
    /*let moderationCommandTitle = document.getElementById(`moderation-commands-titles-${x}`);
    moderationCommandTitle.addEventListener('click', toggleItem, false);
    function toggleItem() {
        let itemClass = this.parentNode.className;
        if(itemClass === 'moderation-commands-bars close') {
            this.parentNode.className = 'moderation-commands-bars open';
        } else {
            this.parentNode.className = 'moderation-commands-bars close';
        }
    }*/
}

export default InteractionMenu;